import { ProsePage, P } from "@/components/prose-page"

export default function TheLoopPage() {
  return (
    <ProsePage
      eyebrow="The Method"
      title="The name is the method"
      backHref="/method"
      backLabel="The Method"
    >
      <P>
        At the helm of an accelerating boat, the wind on your face hardens — not because the weather changed, but because you did. Forward speed adds itself vectorially to the true wind; the rig sees stronger air at a tighter angle, and makes more drive. Sailors call the result apparent wind. The practice is named after it because every line we draw serves that loop, and because the loop has limits that are as much a part of the design as the gains.
      </P>
    </ProsePage>
  )
}
